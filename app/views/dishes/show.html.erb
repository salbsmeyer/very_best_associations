<div class="page-header">
  <h3>Dish #<%= @dish.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">
      <dt>Name</dt>
      <dd><%= @dish.name %></dd>

      <dt>Cuisine</dt>
      <dd><%= @dish.cuisines.name %></dd>

        <dt>Venues</dt>
      <dd>
      <ul class="list-group">
        <% Venue.where(:dish_id=> @dish.id).each do |venues| %>
        <li class="list-group-item">
          <%= venues.name %>
        </li>
        <% end %>
      </ul>
      </dd>
<!-- favorite dishes; print venue names and usernames -->

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">
      <dt>Dish</dt>
      <dd><%= @dish.name %></dd>
      <dt>Favorites</dt>
      <dd><ul class="list-group">
      <% @dish.favorites.each do |favorite| %>
      <li class="list-group-item">
      <%= favorite.venue.name %> : <%= favorite.user.id %>
      </li>
      <% end %>
      </ul>
      </dd>
      </dl>
      </div>


<button class="btn btn-success">
  Create Favorite
</button>

    <div class="btn-group btn-group-justified">
      <a href="/dishes" class="btn btn-primary">
        Back
      </a>
      <a href="/dishes/<%= @dish.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_dish/<%= @dish.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    </div>
  </div>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/dishes" class="btn btn-primary">
        Back
      </a>
      <a href="/dishes/<%= @dish.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_dish/<%= @dish.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    </div>
  </div>
</div>
